import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            List {
                Text("Settings coming soon")
            }
            .navigationTitle("Settings")
        }
    }
}

#Preview {
    SettingsView()
} 